= simple_form_for @comment, url: post_comments_path(resource_post) do |f|
  - if ancestry
    = f.hidden_field :ancestry, value: ancestry
  = f.input :content, label: 'Comment', input_html: { class: 'form-control' }
  = f.button :submit, class: 'btn btn-outline-success'
